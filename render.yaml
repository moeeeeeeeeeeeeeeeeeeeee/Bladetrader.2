services:
  - type: web
    name: bladetrader-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn finhack.api:app --app-dir src --host 0.0.0.0 --port $PORT
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
