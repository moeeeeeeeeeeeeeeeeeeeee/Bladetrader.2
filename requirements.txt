# BladeTrader — Python backend (API, agents, ML)
# Install: python -m pip install -r requirements.txt
# Python 3.11+ recommended.

fastapi>=0.115.0,<1
uvicorn[standard]>=0.32.0,<1
pydantic>=2.10.0,<3
python-dotenv>=1.0.0,<2
python-multipart>=0.0.22,<1
httpx>=0.27.0,<1

# Market data + tabular pipelines
yfinance>=0.2.40,<1
lxml>=6.0.0,<7
pandas>=2.2.0,<3
numpy>=1.26.0,<3

# Algorithms / features
scikit-learn>=1.5.0,<2
lightgbm>=4.3.0,<5

# FinBERT-based document sentiment encoder. The encoder gracefully falls
# back to a lexicon scorer if these are absent, so they are listed but
# can be omitted on machines that don't want a torch install.
transformers>=4.40.0,<5
torch>=2.2.0,<3
