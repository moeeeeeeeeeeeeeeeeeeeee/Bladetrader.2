# FinHack26 — Python backend (API, agents, ML)
# Install: python -m pip install -r requirements.txt
# Python 3.11+ recommended.

fastapi>=0.115.0,<1
uvicorn[standard]>=0.32.0,<1
pydantic>=2.10.0,<3
python-dotenv>=1.0.0,<2
python-multipart>=0.0.22,<1
httpx>=0.27.0,<1

# Market data: Yahoo (dev/testing) + pandas for pipelines / charts API
yfinance>=0.2.40,<1
lxml>=6.0.0,<7
pandas>=2.2.0,<3
numpy>=1.26.0,<3
matplotlib>=3.9.0,<4

# Algorithms / features (expand: xgboost, etc.)
scikit-learn>=1.5.0,<2

# Wireframe PDF generator (scripts/generate_ui_wireframes_pdf.py)
reportlab>=4.2.0,<5
